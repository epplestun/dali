import assert from 'assert';
import sinon from 'sinon';
import jsdom from 'mocha-jsdom';

describe('Filter', () => {
  it('...');
});

describe('Filters', () => {
  describe('#get()', () => {
    it('...');
  });

  describe('#getFilters()', () => {
    it('...');
  });
});