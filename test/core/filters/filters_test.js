var assert = require('assert'),
    sinon = require('sinon'),
    jsdom = require('mocha-jsdom');

describe('Filter', () => {
  it('...');
});

describe('Filters', () => {
  describe('#get()', () => {
    it('...');
  });

  describe('#getFilters()', () => {
    it('...');
  });
});